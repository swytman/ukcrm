- page_title("Администрирование | Группа #{@group.title}")

%ul.breadcrumb
  %li= link_to 'Управление группами', groups_path
  %li #{@group.title}

.row.h1.bottom20.top20
  .col-sm-9.col-sm Группа: #{@group.title}
  .col-sm-3.col-sm
    = link_to 'Удалить', group_path(@group), method: :delete, class: 'btn btn-danger pull-right', data: {confirm: 'Удалить группу'}

= render partial: 'form'
