%nav.navbar.navbar-default
  .container-fluid
    / Brand and toggle get grouped for better mobile display
    .navbar-header
      %button.navbar-toggle.collapsed{"aria-expanded" => "false", "data-target" => "#bs-example-navbar-collapse-1", "data-toggle" => "collapse", :type => "button"}
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      %a.navbar-brand{:href => root_path} MyVillage.online
    / Collect the nav links, forms, and other content for toggling
    #bs-example-navbar-collapse-1.collapse.navbar-collapse
      - if current_user
        - if current_user.is?(:administrator)
          = render 'layouts/admin/menu'
        - elsif current_user.is?(:manager)
          = render 'layouts/manager/menu'
        - elsif current_user.is?(:settler)
          = render 'layouts/settler/menu'
      = render 'shared/user'

