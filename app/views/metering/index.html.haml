.row
  .col-md-10.col-lg-10.col-xs-7
    %h3 Показания
  .col-sm-2.pull-right.top20
    = link_to "Создать", new_metering_path, class: "btn btn-primary btn-sm"
.row
  .col-sm-12
    - if @items.any?
      %table.table.table-stripped.table-hover
        %thead
          %tr
            %th #
            %th.col-sm-4 Название
            %th.col-sm-4 Показание
            %th.col-sm-4 Счетчик
            %th.col-sm-4 Тариф
            %th.col-sm-4 Итого
        %tbody
          - @items.each_with_index do |item, index|
            %tr
              %td= index+1
              %td= link_to item.title, metering_path(item.id)
              %td= link_to item.value

    - else
      %p Нет измерений

